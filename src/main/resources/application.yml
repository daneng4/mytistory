spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  jpa:
    # 테이블 생성 후에 .sql 파일 실행
    defer-datasource-initialization: true

  h2:
    console:
      enabled: true

